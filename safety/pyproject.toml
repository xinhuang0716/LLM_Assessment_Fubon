[project]
name = "safety"
version = "0.1.2"
description = "A safety evaluation pipeline for large language models."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepteam>=1.0.4",
    "requests>=2.32.5",
    "tqdm>=4.67.1",
]
